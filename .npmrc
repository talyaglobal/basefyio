# Pin pnpm's default isolated node-linker so installs are reproducible across
# machines regardless of a developer's global pnpm config. The hoisted linker
# fails to co-locate esbuild's per-version optional binary (@esbuild/*),
# breaking the Astro/Vite build with a host/binary version mismatch.
node-linker=isolated
